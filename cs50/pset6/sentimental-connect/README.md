# CS50 SQL PSet 6: Sentimental Connect

This problem is the MySQL version of the earlier LinkedIn schema design problem.

Starter file:

- `cs50/pset6/sentimental-connect/schema.sql`

Local MySQL wrapper:

```bash
cd "$(git rev-parse --show-toplevel)"
./bin/mysql
```

Suggested first commands:

```sql
CREATE DATABASE IF NOT EXISTS linkedin;
USE linkedin;
SOURCE /workspace/cs50/pset6/sentimental-connect/schema.sql;
SHOW TABLES;
```
